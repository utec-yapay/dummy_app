android {
            defaultConfig {
                applicationId = "org.yapay.software.dummyapp"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }